#!/bin/bash

export CC=clang

pip="python3 -m pip"
par="--upgrade --user"

$pip install $par pip
for pkg in $($pip list | awk 'FNR>2{print $1}')
do
	echo "======================= $pkg"
	$pip install $par $pkg # | grep -v "already up-to-date" &
done
